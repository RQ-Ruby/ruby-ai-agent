<?xml version="1.0" encoding="UTF-8"?>
<project xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xmlns="http://maven.apache.org/POM/4.0.0"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>com.ruby</groupId>
        <artifactId>travel-agent</artifactId>
        <version>1.0-SNAPSHOT</version>
    </parent>

    <artifactId>travel-agent-model</artifactId>
    <description>数据模型模块：实体类、DTO、VO、枚举</description>

    <dependencies>
        <dependency>
            <groupId>com.ruby</groupId>
            <artifactId>travel-agent-common</artifactId>
        </dependency>

        <!-- MyBatis-Plus 注解（@TableName 等） -->
        <dependency>
            <groupId>com.baomidou</groupId>
            <artifactId>mybatis-plus-spring-boot3-starter</artifactId>
        </dependency>

        <!-- Hutool -->
        <dependency>
            <groupId>cn.hutool</groupId>
            <artifactId>hutool-all</artifactId>
        </dependency>
    </dependencies>
</project>